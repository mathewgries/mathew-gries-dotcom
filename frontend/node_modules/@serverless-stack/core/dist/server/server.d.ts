/// <reference types="node" />
declare type StartOpts = {
    port: string;
    host: string;
    rootDir: string;
};
declare type Payload = {
    event: any;
    context: any;
    timeoutAt: string;
};
export declare function start(opts: StartOpts): {
    server: import("http").Server;
    invoke(fun: string, payload: Payload): Promise<string>;
};
export {};
